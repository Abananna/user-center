spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/user_center?serverTimezone=UTC
    username: root
    password:
  session:
    timeout: 86400
server:
  address: 0.0.0.0

